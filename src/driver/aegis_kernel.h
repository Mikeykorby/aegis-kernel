/* aegis_kernel.h -- kernel-side definitions for the minifilter.
 * Protocol layout is shared with usermode via aegis_proto.h (included
 * after fltKernel.h so kernel types resolve). */
#ifndef AEGIS_KERNEL_H
#define AEGIS_KERNEL_H

#include <fltKernel.h>
#include "aegis_proto.h"   /* shared port name + AEGIS_EVENT layout */

#endif /* AEGIS_KERNEL_H */
